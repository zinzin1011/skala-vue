# Code Challenge - env (10. Vite Build & Deployment)
# 스테이징 빌드에서만 쓰이는 값. API 키 같은 비밀값이 아니라 "빌드 모드에 따라
# 엔드포인트가 바뀐다"는 걸 보여주기 위한 예시 URL이라 커밋해도 안전하다.
VITE_API_URL=https://api-stage.skcc.com
VITE_APP_MODE=Staging Mode

# 이 파일을 복사해서 .env 로 저장한 뒤, 실제 발급받은 키를 넣으세요.
#
#   cp .env.example .env
#
# .env 는 .gitignore에 등록돼 있어 커밋되지 않습니다.
# 이 파일(.env.example)은 커밋되므로 실제 키를 넣으면 안 됩니다 — Public 저장소에서는
# 누구나 볼 수 있습니다.
#
# 발급 방법: https://openweathermap.org 가입 → My API Keys 탭에서 확인
VITE_OPENWEATHER_API_KEY=여기에_발급받은_32자리_키
